/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 *
 */

import type { ImportDeclaration } from 'estree';
export type ValidImportSource = string | { from: string; as: string };
type ImportTracker = {
  ImportDeclaration: (node: ImportDeclaration) => void;
  isStylexDefaultImport: (name: string) => boolean;
  isStylexNamedImport: (importName: string, name: string) => boolean;
  clear: () => void;
};
declare function createImportTracker(
  importsToLookFor: Array<ValidImportSource>,
): ImportTracker;
export default createImportTracker;
