"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = makeVariableCheckingRule;
function makeVariableCheckingRule(rule) {
  const varCheckingRule = (node, variables, prop, context) => {
    if (node.type === 'TSSatisfiesExpression' || node.type === 'TSAsExpression') {
      return varCheckingRule(node.expression, variables, prop, context);
    }
    if (node.type === 'Identifier' && variables != null) {
      const existingVar = variables.get(node.name);
      if (existingVar === 'ARG') {
        return undefined;
      }
      if (existingVar != null) {
        return varCheckingRule(existingVar, variables, prop, context);
      }
    }
    return rule(node, variables, prop, context);
  };
  return varCheckingRule;
}